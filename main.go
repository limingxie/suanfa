package main

import (
	"testCode/suanfa/tsearch"
)

func main() {
	tsearch.ErfenchazhaoTest(30)
	/*
		// tsort.BucketSort()
		// tsort.SelectionSort1()
		// tsort.InsertionSort1()
		// tsort.ShellSort1()
		// tsort.QuickSort_b()
		// tsort.BubbleSort()
		// tsort.QuickSort()
		// tqueue.Queuefunc()
		// tqueue.Stackfunc()
		// tqueue.LinkedListFunc()
		// tenum.Enum1()
		// tenum.Enum_huochai()
		// fmt.Println(tenum.Enum3(171))
		// tenum.Enum4()
		// tsearch.DFS()
		slice := make([]int, 3, 10)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		slice = append(slice, 1)
		// slice = append(slice, 1)
		// slice = append(slice, 2)
		// slice[0] = 0
		// slice[1] = 1
		fmt.Printf("%+v", len(slice))
		fmt.Println("")

		hash := make(map[int]bool, 10)
		hash[0] = true
		hash[1] = false
		hash[2] = true
		fmt.Printf("%+v", slice)
		fmt.Println("")
		fmt.Printf("%+v", hash)
	*/
}
